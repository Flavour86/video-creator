import { PageChrome } from "@/components/app-shell/PageChrome";

export default function SetupPage() {
  return (
    <PageChrome variant="empty">
      <h1 className="vc-type-display text-(--text)">Setup</h1>
      <p className="vc-type-body text-(--text-2)">Project setup starts in the next milestone tasks.</p>
    </PageChrome>
  );
}
