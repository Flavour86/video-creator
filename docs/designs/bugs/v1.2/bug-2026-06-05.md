# Bug Report - v1.2 - 2026-06-05

Tested against: `docs/designs/tasks/v1.2/test-plan.md`
Resolutions: `1920x1080` | `1280x720` | `1080x1920`
Summary: 4/4 items clean

Browser sweep source: `rtk pnpm -F @vc/web test:e2e -- render-correctness` exercised the v1.2 integrated flow in a real browser at all three required resolutions and produced the evidence screenshots copied into `docs/designs/bugs/v1.2/evidence/`. Visual parity source: `rtk pnpm -F @vc/web test:visual -- editor` passed all four v1.2 canonical reference cases at the configured `SSIM >= 0.98` threshold.

## Item 1 - Manual Background Coverage Scheduling

- 1920x1080 : PASS  SSIM >=0.98  evidence/background-manual-coverage-1920x1080.png
- 1280x720  : PASS  SSIM >=0.98  evidence/background-manual-coverage-1280x720.png
- 1080x1920 : PASS  n/a          evidence/background-manual-coverage-1080x1920.png
Severity: none
Repro:
  1. Open `/editor/<project_id>` with the v1.2 four-background fixture.
  2. Open `Change Background`, set `ramen-shop.jpg` to `00:00-00:00`, save, reload, and reopen.
Notes: No aspect-matching 9:16 canonical background modal reference exists, so the portrait result was judged by behavior and screenshot presence.

## Item 2 - Subtitle Maximum Characters Control

- 1920x1080 : PASS  SSIM >=0.98  evidence/subtitles-max-characters-1920x1080.png
- 1280x720  : PASS  SSIM >=0.98  evidence/subtitles-max-characters-1280x720.png
- 1080x1920 : PASS  SSIM >=0.98  evidence/subtitles-max-characters-1080x1920.png
Severity: none
Repro:
  1. Open `/editor/<project_id>`.
  2. Open `Subtitles`, set `Max characters per line` to `20`, apply, reload, and reopen.
Notes: Landscape evidence maps to `subtitles-max-characters-16x9.png`; portrait evidence maps to `subtitles-max-characters-9x16.png`.

## Item 3 - Whole-Second Preview Transport Timecodes

- 1920x1080 : PASS  SSIM >=0.98  evidence/editor-time-display-1920x1080.png
- 1280x720  : PASS  SSIM >=0.98  evidence/editor-time-display-1280x720.png
- 1080x1920 : PASS  n/a          evidence/editor-time-display-1080x1920.png
Severity: none
Repro:
  1. Open `/editor/<project_id>`.
  2. Seek or set playback to `38.399s`.
  3. Inspect the preview transport.
Notes: No aspect-matching 9:16 canonical transport reference exists, so the portrait result was judged by behavior and screenshot presence.

## Integrated Regression - Save, Reload, And Render

- 1920x1080 : PASS  n/a  evidence/integrated-flow-1920x1080.png
- 1280x720  : PASS  n/a  evidence/integrated-flow-1280x720.png
- 1080x1920 : PASS  n/a  evidence/integrated-flow-1080x1920.png
Severity: none
Repro:
  1. Create the manual background zero row and gap.
  2. Set subtitle max characters to `20`.
  3. Reload the editor and click `Render final`.
Notes: This item is behavioral evidence only; the changed surfaces are covered by the item-specific visual references above.

## Verdict

CLEAN - all items passed at all three resolutions.
