# Bug Report - v1.3 - 2026-06-15

Tested against: `docs/designs/tasks/v1.3/test-plan.md`
Resolutions: `1920x1080` | `1280x720` | `1080x1920`
Summary: 1/1 items clean

Commands:
- `rtk pnpm test`
- `rtk pnpm lint`
- `rtk pnpm build`
- `rtk pnpm -F @vc/web test`
- `rtk pnpm -F @vc/server test`
- `rtk pnpm -F @vc/web test:visual -- editor`
- `rtk pnpm -F @vc/web test:e2e -- editor-fullscreen`

## Item 1 - Preview Fullscreen Icon Button

- `1920x1080`: PASS  SSIM `0.9895`  `evidence/editor-fullscreen-button-1920x1080.png`
- `1280x720`: PASS  n/a  `evidence/editor-fullscreen-button-1280x720.png`
- `1080x1920`: PASS  n/a  `evidence/editor-fullscreen-button-1080x1920.png`

Severity: none

Repro:
1. Open `/editor/p_v13_fullscreen` in Chromium.
2. Confirm the preview transport shows Previous, Play/Pause, Next, the fullscreen icon-only button, and the timecode.
3. Confirm the fullscreen button is immediately before the timecode and is named `Fullscreen preview`.
4. Click the button, then click it again while fullscreen is active.
5. Switch editor resolutions through `1080p`, `720p`, and `9:16`.
6. Repeat at `1920x1080`, `1280x720`, and `1080x1920`.

Notes:
- Browser instrumentation recorded `requestFullscreen` on `data-testid="preview-stage"`.
- Browser instrumentation recorded `document.exitFullscreen()` on the second toggle.
- The button stayed visible and aligned next to the timecode at all three sweep resolutions.
- The button stayed `32x32` with the expected accessible name and title.
- The e2e flow reported no console errors and no failed `/api/server/` responses.
- `1920x1080` SSIM comes from the v1.3 visual parity case against `docs/designs/tasks/v1.3/visuals/editor-fullscreen-button-1920x1080.png`.
- `1280x720` and `1080x1920` use browser screenshot plus placement and behavior assertions; no separate canonical reference exists for those viewport captures.

## Verdict

CLEAN - all items passed at all three resolutions.
