1. subtitle modal, replace the `Max chars / line` with `Color`, it uses to set the color of subtitle. 'Background' field doesn't have the ability to set background color, opacity and radius of the background rectangle
2. Editor interface removes the Save button, the automated save function runs in the back stage. Once the config of the video changes, the program save it asynchrously
3. add entries to set position of watermark, opacity, scale

